<resources>

    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorAccent</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="AppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="colorControlNormal">@color/hitam</item>
    </style>

    <style name="RatingBar" parent="Theme.AppCompat">
        <item name="colorControlNormal">@color/hitam</item>
        <item name="colorControlActivated">@color/kuning2</item>
    </style>

    <style name="RatingBarOrange" parent="Theme.AppCompat">
        <item name="colorControlNormal">@color/hitam</item>
        <item name="colorControlActivated">@color/kuningbintang</item>
    </style>

    <style name="ProgressBar" parent="Theme.AppCompat">
        <item name="colorControlNormal">@color/abu</item>
        <item name="colorControlActivated">@color/putih</item>
    </style>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light">
        <item name="colorControlNormal">@color/hitam</item>
    </style>

</resources>